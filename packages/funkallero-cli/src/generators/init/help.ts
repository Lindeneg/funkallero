const HELP = `Funkallero Init Generator

$ funkallero init NAME [--OPTIONS]

NAME:     Project name
OPTIONS:  (auth, prisma, zod, npm, yarn)

Examples:

$ funkallero init my-project

$ funkallero init my-project --auth --prisma --zod --npm
`;

export default HELP;

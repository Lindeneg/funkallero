{
    "name": "funkallero",
    "private": "true",
    "repository": "git@github.com:Lindeneg/funkallero.git",
    "author": "lindeneg <christian@lindeneg.org>",
    "license": "MIT",
    "scripts": {
        "bootstrap": "yarn install && lerna bootstrap",
        "bootstrap:ci": "yarn install --frozen-lockfile && lerna bootstrap",
        "reset": "lerna exec --parallel -- rm -rf dist lib node_modules coverage && rm -rf yarn.lock node_modules coverage",
        "cli": "bash ./scripts/cli.sh",
        "build": "yarn cli build",
        "start": "yarn cli start",
        "lint": "yarn cli lint",
        "test": "yarn cli test",
        "docs:start": "yarn --cwd ./docs start",
        "docs:install": "yarn --cwd ./docs install",
        "test:bench": "npx -y start-server-and-test test:bench:start-server http://localhost:3000/api/authors test:bench:start-tests",
        "test:bench:install": "pip3 install -r ./locust/requirements.txt",
        "test:bench:start-server": "node ./e2e/dist/index.mjs",
        "test:bench:start-tests": "bash ./scripts/bench.sh"
    },
    "workspaces": [
        "example",
        "e2e",
        "packages/*"
    ],
    "devDependencies": {
        "@rollup/plugin-commonjs": "22.0.2",
        "@rollup/plugin-node-resolve": "14.1.0",
        "@types/node": "17.0.25",
        "lerna": "6.5.1",
        "prettier": "2.8.0",
        "rollup": "2.79.1",
        "rollup-plugin-cleaner": "1.0.0",
        "rollup-plugin-typescript2": "0.34.1",
        "typescript": "4.9.5"
    }
}
